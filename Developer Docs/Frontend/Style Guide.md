
We will be using *Prettier* as part of our CI/CD pipeline to automatically format frontend code upon creating a *pull request*.
- This means all code will be formatted before collaborating significantly with others
- It also means we don't need to worry about individual Prettier configurations with the potential for config mismatch.

